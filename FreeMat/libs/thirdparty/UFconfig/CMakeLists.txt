CMAKE_MINIMUM_REQUIRED(VERSION 2.6 FATAL_ERROR)
IF(NOT MSVC)
	INSTALL(FILES UFconfig.h DESTINATION include)
ELSE(NOT MSVC)
	INSTALL(FILES UFconfig.h DESTINATION ${PROJECT_SOURCE_DIR}/bin)
ENDIF(NOT MSVC)

