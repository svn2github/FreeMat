% Regression test function (black blox) for FreeMat v4.0
% This function is autogenerated by helpgen.
function bbtest_success = bbtest_eval
  bbtest_success = 1;
NumErrors = 0;
try
  eval('a = 32')

catch
  NumErrors = NumErrors + 1;
end
try
  b = eval('a')

catch
  NumErrors = NumErrors + 1;
end
if (NumErrors ~= 0) bbtest_success = 0; return; end
NumErrors = 0;
try
  s = ['b = a' ' + 2']

catch
  NumErrors = NumErrors + 1;
end
try
  eval(s)

catch
  NumErrors = NumErrors + 1;
end
if (NumErrors ~= 0) bbtest_success = 0; return; end
NumErrors = 0;
try
  a = 32

catch
  NumErrors = NumErrors + 1;
end
try
  b = eval('a','1')

catch
  NumErrors = NumErrors + 1;
end
try
  b = eval('z','a+1')

catch
  NumErrors = NumErrors + 1;
end
if (NumErrors ~= 0) bbtest_success = 0; return; end
