% Regression test function (black blox) for FreeMat v4.0
% This function is autogenerated by helpgen.
function bbtest_success = bbtest_loglog
  bbtest_success = 1;
NumErrors = 0;
try
  x = linspace(1,100);

catch
  NumErrors = NumErrors + 1;
end
try
  y = x;

catch
  NumErrors = NumErrors + 1;
end
try
  plot(x,y,'r-');

catch
  NumErrors = NumErrors + 1;
end
try
  mprint loglog1

catch
  NumErrors = NumErrors + 1;
end
if (NumErrors ~= 0) bbtest_success = 0; return; end
NumErrors = 0;
try
  loglog(x,y,'r-');

catch
  NumErrors = NumErrors + 1;
end
try
  mprint loglog2

catch
  NumErrors = NumErrors + 1;
end
if (NumErrors ~= 0) bbtest_success = 0; return; end
