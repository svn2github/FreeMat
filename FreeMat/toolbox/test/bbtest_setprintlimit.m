% Regression test function (black blox) for FreeMat v4.0
% This function is autogenerated by helpgen.
function bbtest_success = bbtest_setprintlimit
  bbtest_success = 1;
NumErrors = 0;
try
  A = randn(512);

catch
  NumErrors = NumErrors + 1;
end
try
  setprintlimit(10)

catch
  NumErrors = NumErrors + 1;
end
try
  A

catch
  NumErrors = NumErrors + 1;
end
try
  setprintlimit(1000)

catch
  NumErrors = NumErrors + 1;
end
if (NumErrors ~= 0) bbtest_success = 0; return; end
