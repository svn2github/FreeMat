% Regression test function (black blox) for FreeMat v4.0
% This function is autogenerated by helpgen.
function bbtest_success = bbtest_symvar
  bbtest_success = 1;
NumErrors = 0;
try
  symvar('x^2+sqrt(x)')  % sqrt is eliminated as a function

catch
  NumErrors = NumErrors + 1;
end
try
  symvar('pi+3')         % No identifiers here

catch
  NumErrors = NumErrors + 1;
end
try
  symvar('x + t*alpha')  % x, t and alpha

catch
  NumErrors = NumErrors + 1;
end
if (NumErrors ~= 0) bbtest_success = 0; return; end
