{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {cancel = id; }; 
            CLASS = ScriptExecController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                cancelButton = id; 
                messageTextField = id; 
                progressBar = id; 
                progressWindow = id; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}