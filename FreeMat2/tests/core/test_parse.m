k = 0 %

for (i=1:10) % for loop starts
   k = k + 1 % k adds one
end          % loop over all i

method = 0;

if (method==0) % use method zero
  printf('method is zero\n');
end

% Hello
% Goodbye

a = rand(1,10); % Another comment

plot(a) % plot the graph
